import React from 'react';
import { Box } from '@mui/material';

const ProductDetailInfo = () => {
    return (
        <Box className="productDetailInfo">
            <div />
        </Box>
    );
};

export default ProductDetailInfo;

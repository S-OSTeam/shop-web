/* eslint-disable */
const DescUtil = () => {
    /** 숫자 패턴 */
    const patternNumber = /[0-9]/;
    /** 영문 패턴 */
    const patternAlphabet = /[a-zA-Z]/;
    /** 한글 패턴 */
    const patternHangul = /[ㄱ-ㅎ|ㅏ-ㅣ|가-힣]/;
    /** 역순 정렬 */
};
export default DescUtil;

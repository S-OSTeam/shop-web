import React from 'react';
import { RemoteElementParams } from '#molecules/table/tableRemote/TableRemote';
import { Box, Table } from '@mui/material';

interface myPrams {
    data: RemoteElementParams[];
}

const TableEditor = ({ data }: myPrams) => {

    // const _temp = props;

    return (
        <Box component='div'>

        </Box>
    );
};

export default TableEditor;
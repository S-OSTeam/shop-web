import React from 'react';
import { ProductInfoProps } from '@templates/product/info/ProductInfoTemplate';
import { Box } from '@mui/material';

const ProductInfo = ({ className, item }: ProductInfoProps) => {
    return (
        <Box>
            <div />
        </Box>
    );
};

export default ProductInfo;

import React from 'react';
// import Dashboard from '@molecules/dashBoard/Dashboard';

interface SubNavProps {}

const SubNav = ({}: SubNavProps) => {
    // 컴포넌트별 변수로 저장해서 뿌리기
    // const UserComponent = <Dashboard className={} btnClsN={} btnChildren={} menuClsN={} menuChildren={} />;

    return (
        <nav>
            <ul>this is navItemList</ul>
        </nav>
    );
};

export default SubNav;
